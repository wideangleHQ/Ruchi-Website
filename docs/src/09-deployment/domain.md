# Domain & DNS

**Not verified in the current codebase.** No domain configuration, DNS records, or hosting-platform project settings are present in this repository — they would live in the hosting platform's dashboard (e.g. Vercel), not in code.

Any production domain implied by UI copy or fallback URLs in this codebase — for example the placeholder `ruchi-foodline.myshopify.com` used as the client cart's fallback checkout URL (see [Cart](../03-shopify/cart.md)) — is **not confirmed to be the real store domain.** It appears to follow the naming pattern of the mock data rather than a verified production value.

See also: [Deployment](overview.md), [Vercel](vercel.md).
